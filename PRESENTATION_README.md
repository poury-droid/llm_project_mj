# NextStep 발표용 기능 소개

## 프로젝트 개요

NextStep은 취업 준비 과정에서 발생하는 지원 공고, 전형 일정, 체크리스트, 자격증, 공부 계획을 한 곳에서 관리하는 웹 서비스입니다.

사용자는 여러 회사의 채용 일정을 따로 기록하지 않아도 되고, 필기시험이나 자격증 준비에 필요한 공부 계획까지 서비스 안에서 바로 생성하고 관리할 수 있습니다.

## 담당 기능 요약

제가 맡은 핵심 기능은 다음과 같습니다.

- 로그인한 사용자별 데이터 분리
- 자격증 및 시험 정보 등록, 수정, 삭제
- 필기시험 기반 공부 계획 생성
- 개인 시험 기반 공부 계획 생성
- 공부 계획 상세 관리 및 재분배
- Vercel 배포 환경에서 프론트엔드와 백엔드 API 연결 안정화

## 1. 사용자별 데이터 관리

로그인한 사용자만 자신의 데이터를 조회하고 수정할 수 있도록 구성했습니다.

### 주요 흐름

```text
로그인
-> 인증 토큰 확인
-> API 요청 시 사용자 정보 확인
-> user_id 기준으로 데이터 조회 및 저장
```

### 적용 대상

- 지원 공고
- 체크리스트
- 공부 계획
- 자격증 및 시험 정보

이 기능을 통해 여러 사용자가 같은 서비스를 사용해도 서로의 데이터가 섞이지 않도록 했습니다.

## 2. 자격증 및 시험 정보 관리

사용자가 보유한 자격증이나 준비 중인 시험 정보를 등록할 수 있는 기능입니다.

### 제공 기능

- 자격증명 또는 시험명 입력
- 등급, 점수, 발급기관 입력
- 취득일, 만료일 관리
- 메모 작성
- 등록된 자격 정보 수정 및 삭제

### 사용자 시나리오

```text
자격증 페이지 이동
-> 자격증명 입력
-> 점수, 등급, 취득일 입력
-> 자격증 추가
-> 등록 목록에서 확인
```

이 정보는 사용자의 취업 준비 현황을 정리하고, 이후 지원 자격이나 스펙 분석에 활용할 수 있도록 설계했습니다.

## 3. 공부 계획 생성

필기시험 날짜와 과목 정보를 바탕으로 공부 계획을 자동 생성하는 기능입니다.

### 입력 정보

- 시험 날짜
- 평일 공부 가능 시간
- 주말 공부 가능 시간
- 공부 가능한 요일
- 제외할 날짜
- 과목별 범위와 중요도

### 생성 결과

- 날짜별 공부 블록
- 과목별 학습 범위
- 예상 공부 시간
- 복습 일정
- 전체 진행률
- 과목별 진행률

### 사용자 시나리오

```text
공부 계획 만들기 클릭
-> 시험 정보 입력
-> 공부 가능 시간 입력
-> 과목과 범위 입력
-> 자동 계획 생성
-> 오늘의 공부 계획에서 확인
```

단순히 날짜만 나누는 것이 아니라, 사용자가 공부할 수 있는 시간과 제외 날짜를 반영해서 실제로 수행 가능한 계획을 만드는 데 초점을 두었습니다.

## 4. 공부 계획 상세 관리

생성된 공부 계획은 이후에도 사용자가 직접 관리할 수 있습니다.

### 제공 기능

- 날짜별 공부 항목 확인
- 공부 항목 완료 처리
- 공부 항목 추가
- 공부 항목 수정
- 공부 항목 삭제
- 남은 일정 기준 재분배

### 재분배 기능

공부 계획을 진행하다 보면 일정이 밀릴 수 있습니다. 이때 완료된 항목은 유지하고, 남은 항목만 다시 배치할 수 있도록 구현했습니다.

```text
완료된 공부 항목 유지
-> 미완료 항목 수집
-> 남은 날짜와 가능 시간 계산
-> 남은 공부량 재배치
```

이 기능을 통해 사용자가 계획을 처음부터 다시 만들지 않고도 현실적인 일정으로 조정할 수 있습니다.

## 5. 배포 환경 API 연결 개선

로컬 개발 환경과 Vercel 배포 환경은 API 주소가 다르게 동작합니다. 이 차이 때문에 배포 후 CORS 오류가 발생할 수 있어 API 연결 방식을 개선했습니다.

### 개선 전

```text
배포된 프론트엔드 -> https://현재도메인:4000/api 호출
```

배포 환경에서는 4000번 포트의 로컬 서버가 없기 때문에 요청이 실패하거나 CORS 오류가 발생할 수 있었습니다.

### 개선 후

```text
로컬 환경 -> http://localhost:4000/api
배포 환경 -> /api
```

Vercel의 rewrite 설정을 활용해 배포 환경에서는 같은 도메인의 `/api`로 요청하도록 수정했습니다.

또한 백엔드 CORS 설정에 Vercel 도메인 패턴을 허용해 preview와 production 배포에서도 API 요청이 정상 동작하도록 했습니다.

## 기술 스택

### Frontend

- React
- React Router
- Vite
- CSS

### Backend

- Node.js
- Express
- PostgreSQL
- Supabase Auth

### Deployment

- Vercel
- Vercel rewrites
- CORS origin 설정

## 주요 API

### 자격증

```text
GET    /api/credentials
POST   /api/credentials
PATCH  /api/credentials/:id
DELETE /api/credentials/:id
```

### 공부 계획

```text
GET    /api/study-plans
POST   /api/study-plans/personal
GET    /api/study-plans/:id
PATCH  /api/study-plans/:id
POST   /api/study-plans/:id/rebalance
DELETE /api/study-plans/:id
```

### 지원 공고 기반 공부 계획

```text
GET    /api/applications/:id/study-plan
POST   /api/applications/:id/study-plan
PATCH  /api/applications/:id/study-plan
DELETE /api/applications/:id/study-plan
```

## 발표 시연 순서

1. 로그인한다.
2. 자격증 페이지에서 자격증 또는 시험 정보를 추가한다.
3. 공부 계획 페이지로 이동한다.
4. 시험 날짜, 공부 가능 시간, 과목 정보를 입력한다.
5. 공부 계획 만들기 버튼을 눌러 자동 계획을 생성한다.
6. 날짜별 공부 항목과 진행률을 확인한다.
7. 일부 항목을 완료 처리한다.
8. 일정 재분배 기능으로 남은 계획이 다시 조정되는 것을 보여준다.

## 기능의 핵심 가치

이 기능은 취업 준비생이 여러 전형과 시험 준비를 따로 관리해야 하는 불편함을 줄이는 데 목적이 있습니다.

특히 공부 계획 기능은 단순 기록이 아니라 사용자의 남은 기간, 공부 가능 시간, 과목 정보를 바탕으로 실제 실행 가능한 일정표를 만들어 준다는 점이 핵심입니다.

## 실행 및 검증

프론트엔드 빌드:

```bash
npm.cmd run build --prefix frontend
```

공부 계획 로직 테스트:

```bash
npm.cmd run test:study --prefix backend
```

검증 결과:

- 프론트엔드 production 빌드 성공
- 공부 계획 테스트 12개 통과
- 백엔드 `app.js` 문법 검사 통과

